<div x-show="currentRoute === 'about' && !loading" x-cloak>
    <div class="bg-white rounded-lg shadow-lg p-8">
        <h2 class="text-3xl font-bold text-gray-800 mb-6">ℹ️ About Component Architecture</h2>
        
        <div class="prose max-w-none">
            <p class="text-gray-600 text-lg mb-4">
                This application now uses a <strong>component-based architecture</strong> similar to React, 
                where each route is separated into its own reusable component file.
            </p>
            
            <h3 class="text-xl font-semibold text-gray-800 mb-3">🗂️ File Structure:</h3>
            <div class="bg-gray-900 text-green-400 p-4 rounded-lg font-mono text-sm mb-6">
views/<br>
├── layouts/<br>
│   └── app.twig          # Base layout with scripts<br>
├── components/<br>
│   ├── home.twig         # Home component<br>
│   ├── posts.twig        # Posts component<br>
│   ├── users.twig        # Users component<br>
│   └── about.twig        # About component<br>
└── spa-modular.twig      # Main SPA that includes components
            </div>
            
            <h3 class="text-xl font-semibold text-gray-800 mb-3">🏗️ Architecture Benefits:</h3>
            <div class="grid md:grid-cols-2 gap-6 mb-6">
                <div>
                    <h4 class="font-semibold text-gray-700 mb-2">🔧 Maintainability</h4>
                    <ul class="text-gray-600 space-y-1">
                        <li>• Each component in separate file</li>
                        <li>• Easy to locate and edit</li>
                        <li>• Clear separation of concerns</li>
                        <li>• Reduced merge conflicts</li>
                    </ul>
                </div>
                <div>
                    <h4 class="font-semibold text-gray-700 mb-2">♻️ Reusability</h4>
                    <ul class="text-gray-600 space-y-1">
                        <li>• Components can be reused</li>
                        <li>• Include in multiple pages</li>
                        <li>• Pass data via context</li>
                        <li>• Template inheritance</li>
                    </ul>
                </div>
            </div>
            
            <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
                <p class="text-blue-800">
                    <strong>🚀 Next Steps:</strong> You can now create new components by adding files to 
                    <code>/views/components/</code> and including them in your main SPA template!
                </p>
            </div>
        </div>
    </div>
</div>
